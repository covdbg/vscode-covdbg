/**
 * How coverage is visualised in the editor.
 * - `line`   — full-line background highlighting
 * - `gutter` — gutter icons only
 * - `both`   — line highlighting + gutter icons
 */
export type RenderMode = 'line' | 'gutter' | 'both';
